// Placeholder service structure for future Cloudinary file uploads / asset storage
export const cloudinaryService = {
  getOptimizedUrl: (publicId: string): string => {
    // future integration logic placeholder
    return publicId;
  },
};
